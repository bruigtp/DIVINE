% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replace_na.R
\name{clean_missing}
\alias{clean_missing}
\title{Replace Missing Values}
\usage{
clean_missing(data, replace_with = "mean")
}
\arguments{
\item{data}{A data frame. The dataset in which missing values should be replaced.}

\item{replace_with}{A character string or a numeric value. Specifies the method or value
used to replace missing values. Options include:
\itemize{
\item \code{"mean"}: Replace with the column's mean value (ignoring NA).
\item \code{"median"}: Replace with the column's median value (ignoring NA).
\item A numeric value: Replace missing values with this specific value.
\item \code{"mode"}: Replace with the most frequent (mode) value for character columns.
}
Defaults to \code{"mean"}.}
}
\value{
A data frame with missing values replaced according to the specified method.
}
\description{
This function replaces missing values (NA) in a dataset with a specified value
or method (such as mean, median, or a custom value). The replacement is performed
column-wise, allowing for both numeric and character columns to be processed.
}
\details{
This function replaces missing values (\code{NA}) in a data frame with a specified method.
The \code{replace_with} parameter can be:
\itemize{
\item \code{"mean"} or \code{"median"} to replace with the mean or median (only for numeric columns).
\item A numeric value, in which case the function replaces all NAs in numeric columns with
that value.
\item \code{"mode"} to replace missing values with the most frequent value in character columns.
}

If you choose \code{"mode"} for character columns, it will replace the missing values with the
most frequent non-missing value in the column.
}
\examples{
# Example replacing NAs with the mean of each numeric column
clean_missing(mtcars, replace_with = "mean")

# Example replacing NAs with a custom numeric value
clean_missing(mtcars, replace_with = 0)

# Example replacing NAs with the mode for character columns
data <- data.frame(
  Category = c("A", "B", NA, "B", "A", NA),
  Value = c(1, NA, 3, NA, 5, 6)
)
clean_missing(data, replace_with = "mode")

}
