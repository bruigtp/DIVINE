% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imput_missing.R
\name{imput_missing}
\alias{imput_missing}
\title{Replace Missing Values}
\usage{
imput_missing(
  data,
  method = list(dplyr::where(is.numeric) ~ "mean"),
  all_na_rm = FALSE
)
}
\arguments{
\item{data}{A data frame. The dataset in which missing values should be imputed.}

\item{method}{A \strong{named} list of formulas specifying \strong{where} and \strong{how} to impute. Each element must be a one-sided formula of the form \verb{<selector> ~ <value>}.  Supported \verb{<value>} options:
\itemize{
\item \code{"mean"}: replace with the column mean (numeric only).
\item \code{"median"}: replace with the column median (numeric only).
\item \code{"mode"}: replace with the most frequent value (character/factor only).
\item A \strong{numeric} constant: replace with that value (numeric columns).
\item A \strong{character} constant: replace with that value (character columns).
}
Defaults to \code{list(where(is.numeric) ~ "mean")}, which imputes \strong{all} numeric columns by their means.}

\item{all_na_rm}{Logical; if \code{TRUE}, remove any rows where \strong{all} columns are \code{NA} \emph{before} imputation. Defaults to \code{FALSE}.}
}
\value{
A tibble with the same columns as \code{data}, but with missing values replaced per your specifications.
}
\description{
This function replaces missing values (NA) in a dataset with a specified value or method (such as mean, median, or a custom value). The replacement is performed column-wise, allowing for both numeric and character columns to be processed.
}
\details{
You can also choose to drop rows that are entirely \code{NA}.
\itemize{
\item The \code{method} argument uses \strong{tidyselect} helpers. For example, \code{where(is.numeric()) ~ "median"} imputes all numeric columns by their medians.
\item If you specify \code{"mode"}, the function computes the most frequent non-\code{NA} value in each selected column.
}
}
\examples{
# Impute all numeric columns by their means:
imput_missing(icu)

# Impute numeric columns by median, and drop all-NA rows:
imput_missing(
  icu,
  method = list(where(is.numeric) ~ "median"),
  all_na_rm = TRUE
)

}
